node
{
  
    stage('create')
    {
        echo "Create a cluster"
        checkout scm
          stage "Build Pex"
          dir ('/home/ec2-user') { 
          sh('build.sh')
}
        
    }
    
    stage('fetch')
    {
        echo "Fetch cluster address"
    }

    stage('build')
    {
        echo "Build a container"
    }
 

}
